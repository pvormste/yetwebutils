name: tests
on: push

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    container: golang:1.13
    steps:
      - uses: actions/checkout@v2
      - name: go test
        run: |
          go test ./...